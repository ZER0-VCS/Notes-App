# FAQ - Часто задаваемые вопросы

## Вопросы о функциональности

### Чем "Активные заметки" отличаются от "Всего заметок"?

**Всего заметок** - это абсолютное количество всех заметок в базе данных, включая:
- ✅ Обычные заметки
- ✅ Закрепленные заметки
- ✅ Удаленные заметки (tombstones)

**Активные заметки** - это только те заметки, которые **не удалены**:
- ✅ Обычные заметки
- ✅ Закрепленные заметки
- ❌ Удаленные заметки (не считаются)

#### Пример:
```
Всего заметок: 120
├── Активных: 114 (обычные + закрепленные)
│   ├── Закреплено: 5
│   └── Обычных: 109
└── Удалено: 6 (tombstones для синхронизации)
```

#### Почему удаленные заметки остаются в базе?

Удаленные заметки сохраняются как **tombstones** (надгробия) для корректной синхронизации между устройствами:

1. **Без tombstones**: Если удалить заметку на устройстве A, при синхронизации с устройством B заметка вернется обратно (т.к. B считает, что у A просто нет этой заметки).

2. **С tombstones**: Удаленная заметка помечается флагом `deleted: true` и синхронизируется на все устройства, что гарантирует корректное удаление везде.

#### Код реализации:

```python
# В src/notes.py
class Note:
    def __init__(self, ..., deleted: bool = False):
        self.deleted = deleted  # False - активная, True - удаленная

# В src/gui.py - подсчет статистики
all_notes = self.store.get_all_notes()

total = len(all_notes)  # Всего заметок (включая удаленные)
active = len([n for n in all_notes if not n.deleted])  # Только активные
deleted = len([n for n in all_notes if n.deleted])  # Только удаленные
```

---

## Вопросы о статистике

### Что показывает панель "Текущая заметка"?

**Панель отображает детальную информацию о выбранной заметке:**

1. **Количество символов** - общее число символов в теле заметки
2. **Количество слов** - разделенных пробелами
3. **Дата изменения** - когда заметка была последний раз изменена

#### Пример:
```
Текущая заметка: 1,234 символов, 89 слов | Дата изменения: 15 ноября 2025
```

#### Особенности подсчета:

- **Символы**: Учитываются все символы включая пробелы, знаки препинания
- **Слова**: Подсчет методом `split()` - разделение по пробелам
- **Форматирование**: Числа форматируются с пробелами (1 234 вместо 1234)
- **Дата**: Преобразуется из ISO формата в читаемый вид

#### Код реализации:

```python
def update_note_info(self):
    """Обновление информации о текущей заметке."""
    if not self.current_note_id:
        self.note_info_label.setText("")
        return
    
    note = self.store.get_note(self.current_note_id)
    
    # Подсчет символов и слов
    char_count = len(note.body)
    word_count = len(note.body.split()) if note.body.strip() else 0
    
    # Форматирование даты
    created_date = datetime.fromisoformat(note.last_modified.replace('Z', '+00:00'))
    date_str = created_date.strftime("%d %B %Y")
    
    # Форматирование чисел с пробелами
    char_formatted = f"{char_count:,}".replace(',', ' ')
    word_formatted = f"{word_count:,}".replace(',', ' ')
    
    info_text = f"Текущая заметка: {char_formatted} символов, {word_formatted} слов | Дата изменения: {date_str}"
    self.note_info_label.setText(info_text)
```

#### Когда обновляется информация?

- ✅ При выборе заметки из списка
- ✅ При изменении текста заметки
- ✅ При создании новой заметки
- ✅ После сохранения заметки
- ❌ Очищается при закрытии/удалении заметки

---

## Технические вопросы

### Как работает закрепление заметок?

1. **Модель данных**: Поле `pinned: bool` в классе `Note`
2. **UI**: Кнопка "📌 Закрепить" / "📌 Открепить"
3. **Сортировка**: Закрепленные всегда сверху при любой сортировке
4. **Синхронизация**: Состояние закрепления синхронизируется между устройствами

```python
# Сортировка с учетом закрепления
notes.sort(key=lambda n: (not n.pinned, n.last_modified), reverse=True)
#                         ^^^^^^^^^^^^
#                         False (закрепленные) идут раньше True (обычных)
```

### Какие режимы сортировки доступны?

| Режим | Описание | Ключ сортировки |
|-------|----------|-----------------|
| **По дате (новые)** | Новые заметки сверху | `last_modified` (desc) |
| **По дате (старые)** | Старые заметки сверху | `last_modified` (asc) |
| **По алфавиту (А-Я)** | От А до Я | `title.lower()` (asc) |
| **По алфавиту (Я-А)** | От Я до А | `title.lower()` (desc) |
| **По размеру** | Большие заметки сверху | `len(body)` (desc) |

**Важно**: При любой сортировке закрепленные заметки остаются сверху!

---

## Исправленные ошибки

### ❌ Ошибка: `NameError: name 'datetime' is not defined`

**Проблема**: Отсутствовал импорт модуля `datetime` в начале файла `src/gui.py`.

**Решение**:
```python
# Было:
import sys
import logging
from pathlib import Path

# Стало:
import sys
import logging
from pathlib import Path
from datetime import datetime, timezone  # ← Добавлено
```

**Где использовался**:
- Метод `toggle_pin()` - установка времени изменения при закреплении
- Метод `update_note_info()` - форматирование даты для отображения

---

## Полезные советы

### Как эффективно использовать закрепление?

1. **Закрепляйте важные заметки** - они всегда будут сверху
2. **Не злоупотребляйте** - слишком много закрепленных = потеря смысла
3. **Комбинируйте с тегами** - закрепляйте + добавляйте тег "важное"
4. **Используйте для TODO** - закрепите список задач на сегодня

### Как следить за ростом заметок?

Используйте панель статистики:
- **Общее количество растет** → база данных увеличивается
- **Удаленных много** → возможно, стоит очистить базу
- **Закрепленных > 10** → пересмотрите важность

### Какой режим сортировки лучше?

Зависит от задачи:
- **По дате (новые)** - для ежедневной работы (по умолчанию)
- **По алфавиту** - для поиска конкретной заметки
- **По размеру** - для поиска больших заметок или удаления мелких

---

## Связанная документация

- [CHANGELOG.md](CHANGELOG.md) - История версий
- [ROADMAP.md](ROADMAP.md) - План развития
- [README.md](../README.md) - Основная документация
- [PROJECT_STRUCTURE.md](../PROJECT_STRUCTURE.md) - Структура проекта
