# Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
проект следует [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

### 💡 Планируется

#### Версия 0.6.0 (Данные и организация) - Приоритет СРЕДНИЙ
**Фокус**: Улучшение работы с большим количеством заметок

- **Импорт данных** (3-4 часа):
  - Из других JSON файлов
  - Из Markdown файлов (массовый импорт)
  - Из Evernote (.enex)
  - Дубликат-детектор при импорте
  
- **Продвинутая сортировка** (2-3 часа):
  - Выпадающий список: По дате создания, по дате изменения, по алфавиту
  - Сортировка по количеству символов
  - Сохранение предпочтения сортировки
  
- **Закрепление заметок** (1-2 часа):
  - Кнопка "📌 Закрепить" для важных заметок
  - Закрепленные заметки всегда наверху списка
  - Визуальное отличие закрепленных заметок

**Итого**: ~6-9 часов разработки

#### Версия 0.7.0 (История и безопасность) - Приоритет СРЕДНИЙ
**Фокус**: Продвинутые возможности для опытных пользователей

- **История версий** (10-12 часов):
  - Сохранение предыдущих версий при изменении
  - Просмотр истории с временной линией
  - Diff между версиями (построчное сравнение)
  - Откат к выбранной версии
  - Ограничение: последние N версий (настраиваемо)
  
- **Шифрование данных** (8-10 часов):
  - AES-256 шифрование тела заметок
  - Мастер-пароль при запуске приложения
  - Опциональное шифрование (per-note)
  - Безопасное хранение ключей (keyring)
  - Смена мастер-пароля

**Итого**: ~18-22 часа разработки

#### Версия 0.8.0+ (Мультимедиа и расширения) - Приоритет НИЗКИЙ
**Фокус**: Богатый контент и дополнительные возможности

- **Прикрепление файлов** (8-10 часов):
  - Вложения изображений, PDF, документов
  - Превью изображений в заметке
  - Управление вложениями (удаление, экспорт)
  - Синхронизация вложений (base64 или отдельные файлы)
  
- **Markdown рендеринг** (6-8 часов):
  - Разделенный вид: редактор + превью
  - Синтаксис Markdown (заголовки, списки, ссылки)
  - Поддержка таблиц и кода
  - Переключение режимов: только редактор / разделенный / только превью
  
- **Продвинутый поиск** (4-5 часов):
  - Регулярные выражения
  - Поиск по дате создания/изменения
  - Комбинированные фильтры (теги + дата + текст)
  - Сохранение поисковых запросов

**Итого**: ~18-23 часа разработки

#### Версия 1.0.0+ (Облачная интеграция) - ДОЛГОСРОЧНЫЙ ПЛАН
**Фокус**: Профессиональная синхронизация и веб-доступ

- **Облачный API** (15-20 часов):
  - Dropbox/OneDrive/Google Drive API с OAuth
  - Прямая загрузка/выгрузка через API
  - Обработка версий облачного хранилища
  - Оффлайн режим с очередью операций
  
- **Веб-версия** (30-40 часов):
  - Веб-интерфейс на Flask/FastAPI
  - Аутентификация пользователей
  - Синхронизация с десктопным приложением
  - Адаптивный дизайн для мобильных устройств

## [0.5.0] - 2025-11-16

### Добавлено
- **GUI выбора темы**:
  - Меню "Вид" → "Тема" с выбором из 4 тем
  - Сохранение выбранной темы в `config.json`
  - Автоматическая загрузка темы при запуске
  - Radio buttons для эксклюзивного выбора
  
- **Диалог настроек**:
  - Создан `settings_dialog.py` с QDialog и тремя вкладками
  - Вкладка "Общие": настройка интервалов автосохранения (3-10 сек) и автосинхронизации (30-300 сек)
  - Вкладка "Редактор": выбор шрифта и размера
  - Вкладка "Оформление": выбор темы через radio buttons
  - Меню "Файл" → "Настройки"
  - Кнопки: Применить, ОК, Отмена
  - Динамическое обновление интервалов таймеров без перезапуска
  
- **Экспорт заметок**:
  - Создан модуль `export.py` с классом `NoteExporter`
  - Экспорт текущей заметки в Markdown с front-matter метаданными
  - Экспорт текущей заметки в Plain Text (чистый текст)
  - Экспорт текущей заметки в HTML с CSS стилями
  - Массовый экспорт всех заметок в ZIP архив
  - Меню "Файл" → "Экспорт" с 6 опциями
  - Безопасные имена файлов (удаление спецсимволов)
  - Диалоги выбора места сохранения

- **Горячее применение настроек**:
  - Метод `apply_theme_live()` для немедленного применения темы
  - Метод `apply_editor_font()` для мгновенного изменения шрифта
  - Обновление интервалов таймеров в реальном времени
  - Все настройки применяются при нажатии "Применить" или "ОК"
  - Не требуется перезапуск приложения для большинства изменений

- **Автоматические тесты**:
  - Файл `test_v050_features.py` с комплексным тестированием
  - 6 автоматических тестов всех функций v0.5.0
  - Тестирование GUI темы, настроек, экспорта, горячего применения
  - Файл `TESTING_v050.md` с руководством по тестированию

### Улучшено
- Загрузка настроек из `config.json` при запуске приложения
- Централизованный метод `_load_config_settings()` для всех настроек
- Меню "Файл" с логической структурой (Экспорт → Настройки → Выход)
- Мгновенная обратная связь при изменении настроек
- UX: пользователь видит изменения без перезапуска

### Изменено
- Горячая клавиша выхода добавлена: `Ctrl+Q`
- `change_theme()` теперь применяет тему сразу вместо сообщения о перезапуске
- MessageBox показывает "Настройки успешно применены!" вместо требования перезапуска

## [0.4.0] - 2025-11-16

### Добавлено
- **Автосохранение заметок**: Автоматическое сохранение через 5 секунд после последнего изменения
  - Визуальная индикация: "💾 Автоматически сохранено в HH:MM:SS"
  - Останавливается при переключении/удалении заметки
- **Поиск по заметкам**: Мощный поиск с горячей клавишей `Ctrl+F`
  - Регистронезависимый поиск по заголовку, тексту и тегам
  - Счётчик результатов: "✅ Найдено заметок: X" / "❌ Ничего не найдено"
  - Визуальные индикаторы типа совпадения:
    - 📌 - найдено в заголовке
    - 📄 - найдено в тексте
    - 🏷️ - найдено в тегах
  - **Подсветка текста**: Найденный текст подсвечивается системным цветом во всех полях
  - Автоматическая прокрутка к первому найденному вхождению
  - Кнопка быстрой очистки поиска
- **Автосинхронизация**: Фоновая синхронизация каждые 60 секунд
  - Автоматически включается при настройке папки облака
  - Работает без модальных окон (не мешает работе)
  - Статус: "✅ Автосинхронизация: X заметок"
- **Теги и категории**: Организация заметок с помощью тегов
  - Поле для ввода тегов через запятую
  - Поиск по тегам интегрирован в общий поиск
  - Метод `get_all_tags()` для получения уникальных тегов
  - Сохранение тегов в JSON и синхронизация между устройствами
- **Система управления темами**: Полноценная инфраструктура для кастомизации внешнего вида
  - Модуль `themes.py` с классами `Theme` и `ThemeManager`
  - 4 предустановленные темы: Светлая, Темная, Голубая, Зеленая
  - Каждая тема содержит полный набор цветов для всех элементов UI
  - Метод `apply_theme()` для программной смены темы
  - Генерация QSS стилей из определений тем
  - Файл `example_themes.py` с примерами использования

### Улучшено
- **UI улучшения**:
  - Длинные заголовки теперь отображают начало, а не конец текста
  - Добавлено визуальное разделение между заметками в списке (4px spacing)
  - Индикаторы результатов поиска для быстрой идентификации
  - **Единообразная подсветка**: Использование системного цвета (QPalette.Highlight) во всех полях
- **Интеграция с ОС**: Подсветка найденного текста использует нативный цвет выделения Windows/macOS/Linux
- Организация проекта: все тесты перенесены в папку `tests/`
- Все новые функции работают с синхронизацией и автосохранением

### Изменено
- Цвет выделения при поиске изменен с желтого (RGB 255,235,59) на системный (QPalette)

## [0.3.0] - 2025-11-16

### Добавлено
- **Синхронизация между устройствами**:
  - Класс `SyncManager` с алгоритмом Last-Writer-Wins (LWW)
  - Обработка конфликтов с созданием заметок-конфликтов (префикс ⚠️)
  - Кнопка "🔄 Синхронизировать" в GUI
  - Кнопка настроек "⚙️" для изменения папки синхронизации
- **Асинхронная синхронизация**: Выполнение в фоновом потоке для предотвращения зависания UI
- **Сохранение конфигурации**: Автоматическое сохранение/загрузка пути к облачной папке в `config.json`

### Изменено
- **Ограничение длины заголовка**: Снижено с 500 до 100 символов (GUI + валидация в модели)
- Улучшена обработка длительных операций (OneDrive downloads) без блокировки UI

### Исправлено
- Зависание интерфейса при синхронизации с OneDrive
- Невозможность изменить папку синхронизации после первоначальной настройки
- Блокировка кнопок синхронизации после завершения операции (использованы Qt сигналы вместо QTimer.singleShot)
- **Растягивание окна за пределы экрана** при открытии заметок с длинными заголовками/текстом:
  - Установлены минимальные (800x500) и максимальные (1600x1200) размеры окна
  - Включен автоматический перенос слов в текстовом редакторе
  - Ограничена максимальная ширина поля заголовка (800px) и списка заметок (400px)
  - Длинные названия в списке обрезаются до 50 символов с добавлением "..."
- **Сдвиг кнопок при обновлении статуса**: Статус-бар перенесен в отдельную строку под кнопками
- **Иконка кнопки выбора папки**: Заменена с ⚙️ (шестеренка) на 📁 (папка) для ясности
- **Возврат удалённых заметок после синхронизации**: Реализованы tombstones (мягкое удаление) для синхронизации удаления между устройствами
- **Конфликтные копии OneDrive**: Автоматическое обнаружение и слияние файлов типа `notes-DESKTOP-XXX.json` и `notes (conflicted copy).json`

## [0.2.0] - 2025-11-15

### Добавлено
- **Обработка ошибок**: Полная обработка исключений в `notes.py` и `gui.py`
- **Логирование**: Система логирования с записью в файл `notes_app.log`
- **Валидация данных**: Метод `Note.validate()` для проверки корректности данных
- **Горячие клавиши**:
  - `Ctrl+S` - Сохранить заметку
  - `Ctrl+N` - Создать новую заметку
  - `Ctrl+D` - Удалить заметку
- **Резервное копирование**: Метод `NoteStore.create_backup()` с хранением последних 10 копий
- **Атомарная запись**: Сохранение через временный файл для предотвращения повреждения данных

### Изменено
- Все вызовы `print()` заменены на `logging`
- Улучшены сообщения об ошибках в GUI
- Обновлен `.gitignore` для исключения логов и резервных копий

### Исправлено
- Обработка повреждённых JSON файлов с автоматическим созданием резервной копии
- Корректная обработка ошибок при чтении/записи файлов

## [0.1.0] - 2025-11-12

### Добавлено
- **MVP (Minimum Viable Product)**:
  - Классы `Note` и `NoteStore` для управления заметками
  - CRUD операции (создание, чтение, обновление, удаление)
  - Хранение в JSON (`~/.notes_app/notes.json`)
  - GUI на PySide6 с редактором и списком заметок
  - Отслеживание несохранённых изменений
  - Подтверждения при критических операциях

### Технические характеристики
- Python 3.13, PySide6
- Модульная архитектура (notes.py, gui.py, main.py)
- Unicode (UTF-8) поддержка
- Версионирование заметок

---

**Легенда**: `Добавлено` | `Изменено` | `Исправлено` | `Удалено` | `Безопасность`
- `Устарело` - для функциональности, которая скоро будет удалена
- `Удалено` - для удалённой функциональности
- `Исправлено` - для исправления ошибок
- `Безопасность` - для изменений, связанных с безопасностью
