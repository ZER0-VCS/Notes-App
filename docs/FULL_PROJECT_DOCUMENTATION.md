# Notes-App — Полная документация проекта


## 1. Обзор проекта

### 1.1. Назначение
Notes-App — это настольное приложение для создания и управления текстовыми заметками с графическим интерфейсом на PySide6. Основной упор сделан на:
- надёжное локальное хранение заметок в JSON;
- безопасную синхронизацию через облачную папку (OneDrive/Dropbox/Google Drive и др.);
- удобный поиск, теги и сортировку;
- кастомизацию внешнего вида (темы, шрифты);
- экспорт и резервное копирование.

### 1.2. Ключевые возможности (в текущей версии 0.6.0)
- CRUD-операции над заметками (создание, чтение, редактирование, удаление).
- Автосохранение через 5 секунд после последнего изменения.
- Автосинхронизация заметок каждые 60 секунд в фоне.
- Мощный поиск по заголовку, тексту и тегам с подсветкой результатов.
- Теги и категории (ввод через запятую, интеграция с поиском).
- Система удаления на основе tombstones (мягкое удаление) с очисткой старых записей.
- Синхронизация через облачную папку с алгоритмом Last-Writer-Wins.
- Автоматическое обнаружение и слияние конфликтных файлов OneDrive.
- Система тем (4 предустановленных темы, ThemeManager, QSS-стили).
- Диалог настроек с тремя вкладками: общие, редактор, оформление.
- Экспорт в Markdown/Plain Text/HTML и массовый ZIP-экспорт.
- Закрепление заметок (pinned) и продвинутая сортировка.
- Статистика по количеству заметок (всего/активных/закреплённых/удалённых).
- Логирование и резервное копирование данных.

### 1.3. Целевая аудитория
- Пользователи, которым нужен простой, оффлайн‑ориентированный менеджер заметок с синхронизацией через файловое облако.
- Разработчики, желающие изучить пример приложения на PySide6 с реальной архитектурой, синхронизацией и тестами.

---

## 2. Требования и окружение

### 2.1. Программные требования
- Python: 3.9–3.13 (рекомендуется 3.13).
- Операционная система: Windows, Linux, macOS (ориентир — кроссплатформенный PySide6).
- Зависимости:
  - PySide6 (Qt GUI)
  - Прочие пакеты из `requirements.txt`.

### 2.2. Структура проекта

Основная структура (объединено из `README.md` и `PROJECT_STRUCTURE.md`):

```text
Notes-App/
├── src/
│   ├── __init__.py
│   ├── gui.py             # Графический интерфейс (PySide6)
│   ├── notes.py           # Модель данных (Note, NoteStore)
│   ├── sync.py            # Синхронизация между устройствами (SyncManager, LWW)
│   ├── themes.py          # Система управления темами (Theme, ThemeManager)
│   ├── export.py          # Экспорт заметок в Markdown/TXT/HTML/ZIP
│   ├── settings_dialog.py # Диалог настроек (3 вкладки)
│   └── example_themes.py  # Примеры использования тем
│
├── docs/
│   ├── README/ROADMAP/CHANGELOG и др.
│   ├── CHANGELOG.md
│   ├── ROADMAP.md
│   ├── CONTRIBUTING.md
│   ├── SYNC_IMPROVEMENTS.md
│   ├── TESTING_v050.md
│   ├── TEST_REPORT_v050.md
│   └── Отчёт по созданию.ini
│
├── tests/
│   ├── test_v050_features.py      # Тесты функций v0.5.0
│   ├── test_fixes_v050.py         # Тесты исправлений v0.5.0
│   ├── test_search.py             # Поиск
│   ├── test_autosave.py           # Автосохранение
│   ├── test_autosync.py           # Автосинхронизация
│   ├── test_auto_create_note.py   # Автосоздание/начальные сценарии
│   ├── test_highlight_all_fields.py
│   ├── test_manual_maximize.py
│   ├── test_maximize_button.py
│   ├── test_sync_search_interaction.py
│   ├── test_tags.py
│   ├── test_tombstones.py
│   ├── test_ui_improvements.py
│   └── __pycache__/
│
├── main.py                 # Точка входа
├── gui.py                  # Переадресует в src/gui.py или содержит оболочку (исторически)
├── requirements.txt        # Зависимости Python
├── notes_app.spec          # Конфигурация PyInstaller
├── BUILD_INSTRUCTIONS.md   # Инструкция по сборке EXE
├── README.md               # Пользовательская документация
└── ... (build/, dist/, логи и пр.)
```

### 2.3. Файлы данных и конфигурации
- JSON‑хранилище заметок: `~/.notes_app/notes.json`.
- Резервные копии: `~/.notes_app/backups/notes_backup_YYYYMMDD_HHMMSS.json` (хранится до 10 копий).
- Лог‑файл: `notes_app.log` в корне проекта или рядом с исполняемым файлом.
- Конфигурация приложения (в т.ч. тема, интервалы, шрифт, облачная папка): `config.json` в профиле пользователя (точный путь задаётся в коде GUI).

---

## 3. Сборка и запуск

### 3.1. Быстрый старт (из исходников)

```powershell
# Клонирование
git clone https://github.com/ZER0-VCS/Notes-App.git
cd Notes-App

# Создание и активация виртуального окружения (Windows PowerShell)
py -3.13 -m venv .venv
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\.venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
python main.py
```

На Linux/macOS: вместо `py -3.13` использовать `python3.13`, активация окружения через `source .venv/bin/activate`.

### 3.2. Сборка standalone EXE (Windows)

См. `BUILD_INSTRUCTIONS.md`:

1. Убедиться, что установлены зависимости (`pip install -r requirements.txt`).
2. Выполнить сборку:

```powershell
python -m PyInstaller notes_app.spec --clean
```

3. Готовый исполняемый файл: `dist/NotesApp.exe`.
4. Для распространения достаточно передать только `NotesApp.exe`.

Особенности:
- onefile GUI‑режим (без консоли);
- в EXE включён интерпретатор Python, PySide6 и прочие зависимости;
- ожидаемый размер EXE: ~50–100 МБ.

---

## 4. Архитектура и модули

### 4.1. Общая архитектура

Высокоуровневая схема (из `README.md` и `PROJECT_STRUCTURE.md`):

```text
┌─────────────┐
│   main.py   │  ← Точка входа
└──────┬──────┘
       │
┌──────▼──────┐
│   gui.py    │  ← GUI (PySide6)
│ NotesApp    │
└──────┬──────┘
       │
┌──────▼──────┐
│  notes.py   │  ← Модель данных (Note, NoteStore)
└──────┬──────┘
       │
┌──────▼──────┐
│  sync.py    │  ← Синхронизация (SyncManager, LWW, tombstones)
└──────┬──────┘
       │
┌──────▼───────────────┐
│ themes.py / export.py│ ← Темы, экспорт
└──────────────────────┘
```

### 4.2. Модуль `notes.py` (модель данных)

Ответственность:
- Описание сущности заметки (`Note`):
  - Основные поля: `id`, `title`, `body`, `last_modified`, `version`, `tags`, `deleted`, `pinned` (в новых версиях).
  - Валидация (`Note.validate()`): корректность данных, допустимые размеры.
- Хранилище заметок (`NoteStore`):
  - CRUD‑операции: создание, обновление, логическое удаление (tombstones), чтение.
  - Загрузка/сохранение структуры данных в JSON (`notes.json`).
  - Атомарная запись через временный файл, чтобы избежать повреждения файла при сбоях.
  - Создание резервных копий (`create_backup()`), хранение последних 10.
  - Методы выборки:
    - активные заметки (без tombstones);
    - с включёнными tombstones (для синхронизации);
    - очистка старых tombstones (`cleanup_tombstones(older_than_days=30)`).

  #### 4.2.1. Пример использования `Note` и `NoteStore`

  ```python
  from notes import Note, NoteStore

  store = NoteStore()                       # создаём/открываем хранилище

  note = Note(title="Идея проекта",       # создаём заметку
        body="Нужно сделать приложение для заметок",
        tags=["идеи", "работа"])

  store.add_note(note)                      # сохраняем её

  note.update(body="Обновлённый текст...")
  store.update_note(note.id, body=note.body)

  for n in store.get_all_notes():           # перечисляем активные заметки
    print(n.id, n.title)
  ```

  Ключевые методы `NoteStore`:
  - `add_note(note)` — добавляет новую заметку (с валидацией).
  - `update_note(note_id, title=None, body=None, tags=None)` — обновляет существующую заметку и увеличивает версию.
  - `delete_note(note_id)` — мягко удаляет заметку, устанавливая `deleted=True` (tombstone).
  - `cleanup_tombstones(older_than_days=30)` — физически удаляет старые tombstones.
  - `get_all_notes()` — возвращает только активные заметки.
  - `get_all_notes_including_deleted()` — возвращает все заметки, включая удалённые.

  #### 4.2.2. Структура данных заметки

  Модель заметки эволюционировала вместе с версиями приложения. Итоговый набор полей:

  | Поле | Тип | Версия | Описание |
  |------|-----|--------|----------|
  | `id` | `str` (UUID) | v0.1.0 | Уникальный идентификатор заметки |
  | `title` | `str` | v0.1.0 | Заголовок заметки (ограничение 100 символов) |
  | `body` | `str` | v0.1.0 | Основной текст заметки (до ~1 МБ) |
  | `last_modified` | `str` (ISO‑datetime) | v0.1.0 | Время последнего изменения, используется в LWW‑алгоритме |
  | `version` | `int` | v0.1.0 | Счётчик версии заметки, увеличивается при обновлении |
  | `deleted` | `bool` | v0.3.0 | Флаг tombstone для мягкого удаления и синхронизации удаления |
  | `tags` | `List[str]` | v0.4.0 | Список тегов, используется в поиске и организации |
  | `pinned` | `bool` | v0.6.0 | Признак закреплённой заметки (отображается сверху списка) |

### 4.3. Модуль `sync.py` (синхронизация)

Ответственность:
- `SyncManager`:
  - Работа с облачной папкой, заданной пользователем.
  - Реализация алгоритма Last‑Writer‑Wins при слиянии заметок:
    - при конфликтах сравнивается `last_modified`, выбирается более свежая версия;
    - поддержка tombstones (если более новая версия помечена deleted, заметка считается удалённой).
  - Асинхронная синхронизация, чтобы не блокировать GUI.
  - Автосинхронизация (по таймеру, по умолчанию 60 сек).

- Обработка конфликтных файлов OneDrive (см. `SYNC_IMPROVEMENTS.md`):
  - Поиск файлов по паттернам `notes-*.json` и имён с `conflicted`.
  - Чтение конфликтных JSON‑файлов и слияние заметок в основной `notes.json`.
  - Удаление конфликтных файлов после успешного слияния.

#### 4.3.1. Алгоритм LWW и пример логики merge

Основной метод слияния — `SyncManager.merge_notes(local_notes, remote_notes)`:
- строит объединённое множество идентификаторов заметок;
- для заметок, существующих только локально или только в облаке, берёт их как есть (включая tombstones);
- для заметок, присутствующих и локально, и удалённо:
  - если хотя бы одна помечена `deleted=True`, побеждает более свежий tombstone;
  - иначе сравнивается `last_modified`, и берётся более свежая версия (LWW);
  - при подозрении на конфликт (изменения почти одновременно и разное содержимое) создаётся `SyncConflict`.

Упрощённая схема решения конфликта:

```python
merged_notes, conflicts = sync_manager.merge_notes(local_notes, remote_notes)

for conflict in conflicts:
    # по умолчанию берём локальную версию,
    # а удалённую сохраняем как отдельную конфликтную заметку
    conflict_note = sync_manager.create_conflict_note(conflict)
    store.add_note(conflict_note)
```

### 4.4. Модуль `themes.py` (темы оформления)

Ответственность:
- Класс `Theme` — описание одной темы (цвета текста, фона, кнопок, выделения, и т.д.).
- Класс `ThemeManager` — загрузка, хранение и применение тем:
  - 4 предустановленных темы: светлая, тёмная, голубая, зелёная.
  - Генерация QSS‑стилей и применение к приложению через `QApplication.setStyleSheet`.
  - API для выбора темы по имени и получения списка доступных тем.

#### 4.4.1. Пример смены темы из кода

```python
from themes import theme_manager

theme_manager.set_theme("dark")
app.setStyleSheet(theme_manager.get_stylesheet())
```

В GUI эта логика обёрнута в методы `NotesApp.change_theme()` и `NotesApp.apply_theme_live(theme_name)`, которые также сохраняют выбранную тему в `config.json`.

### 4.5. Модуль `export.py` (экспорт заметок)

Ответственность:
- Класс `NoteExporter` — экспорт заметок в различные форматы:
  - Markdown (`.md`) с YAML front‑matter (метаданные: заголовок, теги, даты, id).
  - Plain Text (`.txt`) — в основном текст и заголовок.
  - HTML (`.html`) — с базовой CSS‑разметкой.
- Массовый экспорт всех заметок в ZIP‑архив (для резервного копирования/миграции).
- Генерация безопасных имён файлов (удаление спецсимволов).

#### 4.5.1. Пример экспорта заметки

```python
from pathlib import Path
from export import NoteExporter

note = store.get_all_notes()[0]

NoteExporter.export_to_markdown(note, Path("exports/note.md"))
NoteExporter.export_to_txt(note, Path("exports/note.txt"))
NoteExporter.export_to_html(note, Path("exports/note.html"))
```

### 4.6. Модуль `settings_dialog.py` (диалог настроек)

Ответственность:
- `SettingsDialog` (QDialog) с вкладками:
  - «Общие» — интервалы автосохранения (3–10 сек) и автосинхронизации (30–300 сек).
  - «Редактор» — шрифт и размер шрифта для поля текста.
  - «Оформление» — выбор темы (radio‑buttons), синхронизированный с `ThemeManager`.
- Кнопки: «Применить», «ОК», «Отмена».
- Логика сохранения настроек в `config.json` и горячего применения без перезапуска приложения.

### 4.7. Модуль `gui.py` (главное окно)

Ответственность:
- `NotesApp` (QMainWindow):
  - Список заметок слева, редактор и метаданные справа.
  - Кнопки управления: Создать, Сохранить, Закрепить, Удалить, Синхронизировать и др.
  - Поле поиска и индикаторы результатов.
  - Меню:
    - «Файл»: экспорт, настройки, выход (Ctrl+Q).
    - «Вид»: выбор темы.
  - Отрисовка подсветки найденного текста системным цветом (через `QPalette.Highlight`).
  - Логика автосохранения и автосинхронизации (Qt‑таймеры).
  - Статистика (панель внизу): всего/активных/закреплённых/удалённых заметок.

  #### 4.7.1. Общая логика работы GUI

  1. При запуске `NotesApp` инициализирует `NoteStore`, `SyncManager` и `ThemeManager`, загружает конфигурацию (тема, интервалы, шрифт, облачная папка).
  2. Загружаются заметки из локального JSON и отображаются в списке слева с учётом сортировки и закрепления.
  3. При вводе текста/заголовка/тегов:
    - выставляется флаг несохранённых изменений;
    - перезапускается таймер автосохранения (через 5 секунд вызов `autosave_current_note`).
  4. При сохранении заметки (`save_current_note`):
    - данные из полей формы переносятся в объект `Note` и в `NoteStore`;
    - список заметок обновляется, пересчитывается статистика.
  5. При удалении (`delete_current_note`):
    - вызывается `NoteStore.delete_note`, создаётся tombstone вместо физического удаления;
    - заметка скрывается из списка активных.
  6. Поиск (`filter_notes`):
    - фильтрует список заметок по заголовку, тексту и тегам;
    - обновляет метку количества результатов;
    - вызывает подсветку найденного текста в полях редактора.
  7. Синхронизация (`sync_notes` / `auto_sync_notes`):
    - запускает фоновый поток синхронизации через `SyncManager`;
    - по завершении отображает статус (количество синхронизированных и конфликтов);
    - при наличии конфликтов может создавать отдельные конфликтные заметки.

  Ключевые методы GUI, определяющие поведение:
  - `load_notes_list()` — загружает заметки из `NoteStore` в список с учётом сортировки и закрепления.
  - `on_note_selected(item)` — загружает выбранную заметку в редактор.
  - `on_text_changed()` — отслеживание изменений и запуск таймера автосохранения.
  - `toggle_pin()` — переключение флага `pinned` для текущей заметки.
  - `update_statistics()` — пересчёт и отображение статистики по заметкам.

---

## 5. Функциональность по версиям (CHANGELOG)

### 5.1. Версия 0.1.0 (MVP)
- Базовые классы `Note` и `NoteStore`.
- CRUD‑операции и хранение в `~/.notes_app/notes.json`.
- Простой GUI на PySide6 с редактором и списком заметок.
- Отслеживание несохранённых изменений и подтверждение при критических действиях.

### 5.2. Версия 0.2.0
- Полная обработка исключений в `notes.py` и `gui.py`.
- Логирование (`notes_app.log`).
- Валидация данных через `Note.validate()`.
- Горячие клавиши: Ctrl+S, Ctrl+N, Ctrl+D.
- Резервное копирование и атомарная запись.

### 5.3. Версия 0.3.0
- Модуль `sync.py` с `SyncManager` и LWW‑алгоритмом.
- Асинхронная синхронизация без блокировки UI.
- Выбор/изменение облачной папки через GUI.
- Ограничения по размерам окна и элементов, улучшенный UI.
- Введение tombstones и автоматического слияния конфликтных файлов OneDrive (см. `SYNC_IMPROVEMENTS.md`).

### 5.4. Версия 0.4.0
- Автосохранение через 5 секунд после изменений.
- Автосинхронизация каждые 60 секунд.
- Мощный поиск с подсветкой, счётчиком результатов и индикаторами (заголовок/текст/теги).
- Поддержка тегов и их интеграция с поиском.
- Введение модуля `themes.py` и базовой системы тем (Theme/ThemeManager).

### 5.5. Версия 0.5.0
- Меню выбора темы (4 темы) и автозагрузка по `config.json`.
- Диалог настроек с вкладками и горячим применением настроек.
- Экспорт одной и всех заметок в Markdown, TXT, HTML (+ ZIP для массового экспорта).
- Автоматические тесты функциональности v0.5.0 (`test_v050_features.py`).
- Подробная документация по тестированию (`TESTING_v050.md`, `TEST_REPORT_v050.md`).

### 5.6. Версия 0.6.0
- Закрепление заметок (`pinned: bool`) и UI для включения/отключения.
- Статистика по заметкам (всего/активные/закреплённые/удалённые).
- Расширенная сортировка (по дате, алфавиту, размеру текста) с приоритетом закреплённых.
- Полная обратная совместимость с заметками v0.5.0.

---

## 6. Синхронизация и tombstones (из SYNC_IMPROVEMENTS.md)

### 6.1. Tombstones

- При удалении заметки она не удаляется физически сразу, а помечается `deleted=true`.
- Во время синхронизации это состояние передаётся другим устройствам.
- Через 30 дней tombstone может быть полностью удалён, чтобы не засорять хранилище.
- Это решает проблему «возвращения» удалённых заметок после синхронизации.

Пример структуры заметки:

```json
{
  "id": "abc123",
  "title": "Заметка",
  "body": "Текст",
  "last_modified": "2025-11-16T12:00:00Z",
  "version": 2,
  "tags": ["работа", "важное"],
  "deleted": true,
  "pinned": false
}
```

### 6.2. Обработка конфликтов OneDrive

- Обнаружение файлов вида `notes-DESKTOP-*.json` и `notes (conflicted copy).json`.
- В ходе синхронизации:
  - заметки из конфликтных файлов сливаются в основной `notes.json` по LWW;
  - конфликтные файлы удаляются после успешного объединения.
- В лог пишутся детализированные сообщения о ходе слияния и очистке.

---

## 7. Пользовательские сценарии и UX

### 7.1. Основные действия
- Создание заметки — кнопка/горячая клавиша Ctrl+N.
- Редактирование заголовка и текста в правой части окна.
- Сохранение — кнопка или Ctrl+S, плюс автосохранение.
- Удаление — кнопка или Ctrl+D (логическое удаление через tombstones).
- Поиск — поле поиска или Ctrl+F:
  - отображение количества результатов;
  - подсветка найденных фрагментов;
  - индикаторы соответствия в заголовке/тексте/тегах.

### 7.2. Типичные сценарии использования

1. **Работа на двух устройствах с облачной синхронизацией**
  1. На устройстве A пользователь настраивает папку синхронизации (OneDrive/Dropbox/др.).
  2. Создаёт несколько заметок, добавляет теги.
  3. Автосинхронизация или ручная синхронизация выгружает файл `notes.json` в облако.
  4. На устройстве B пользователь указывает ту же папку облака в настройках.
  5. При запуске приложения на устройстве B `SyncManager` загружает заметки из облака и объединяет их с локальными.
  6. Дальнейшие изменения синхронизируются автоматически, при конфликте создаётся отдельная конфликтная заметка.

2. **Быстрый ввод и поиск заметок по тегам**
  1. Пользователь создаёт заметку, вводя заголовок, текст и теги (например: `работа, проект, срочно`).
  2. Позже открывает приложение и нажимает Ctrl+F.
  3. Вводит в поиск слово `срочно` — в списке остаются только релевантные заметки, помеченные индикатором 🏷️ (совпадение по тегам).
  4. Открывает нужную заметку, редактирует её — автосохранение сохраняет изменения через несколько секунд.

3. **Очистка старых удалённых заметок (tombstones)**
  1. Пользователь активно удаляет ненужные заметки; они помечаются как `deleted=True`, но не исчезают из файла сразу.
  2. Через несколько недель автозапуск/ручной вызов логики очистки (`cleanup_tombstones`) удаляет tombstones старше, например, 30 дней.
  3. Файл `notes.json` остаётся компактным, при этом недавние удаления по‑прежнему корректно синхронизируются с другими устройствами.

4. **Подготовка архива заметок для переноса/резервного копирования**
  1. Пользователь выбирает меню «Файл → Экспорт → Все заметки в ZIP (Markdown)».
  2. Указывает папку для сохранения архива.
  3. Приложение создаёт временную директорию, экспортирует каждую заметку в `.md` и упаковывает их в ZIP.
  4. Полученный архив можно хранить как резервную копию или импортировать в другие инструменты (например, Markdown‑редактор или Git‑репозиторий заметок).

### 7.2. Темы и настройки
- Меню «Вид → Тема» позволяет выбрать одну из 4 тем.
- Диалог «Файл → Настройки» даёт:
  - изменение интервалов автосохранения и автосинхронизации;
  - выбор шрифта и размера редактора;
  - выбор темы.
- Кнопка «Применить» обновляет вид и поведение приложения без перезапуска.

### 7.3. Экспорт и резервное копирование
- Экспорт текущей заметки в один из трёх форматов.
- Массовый экспорт всех заметок в ZIP‑архив (Markdown/TXT/HTML).
- Автоматическое резервное копирование файла `notes.json` при ошибках или перед критическими операциями.

### 7.4. Закрепление и сортировка
- У заметки можно включить/выключить флаг `pinned`.
- В списке заметок закреплённые отображаются вверху при любой сортировке.
- Доступные режимы сортировки:
  - по дате (новые сначала);
  - по дате (старые сначала);
  - по алфавиту (А–Я/Я–А);
  - по размеру текста.

---

## 8. Тестирование

### 8.1. Автоматические тесты (общий обзор)

В каталоге `tests/` находятся следующие группы тестов:
- `test_v050_features.py` — комплексное тестирование функциональности v0.5.0 (темы, настройки, экспорт, интервалы).
- `test_fixes_v050.py` — тесты исправлений версии 0.5.0.
- `test_search.py` — поиск и подсветка.
- `test_autosave.py` — автосохранение.
- `test_autosync.py` — автосинхронизация.
- `test_auto_create_note.py` и др. — дополнительные поведенческие и UI‑тесты.
- `test_tombstones.py` — корректность работы tombstones и очистки.
- `test_tags.py` — работа с тегами.

(Полный список см. в `tests/`.)

### 8.2. Тестирование v0.5.0 (по TESTING_v050 и TEST_REPORT_v050)

- Автотест: `python test_v050_features.py` — 6/6 тестов проходят успешно.
- Ручные сценарии:
  - смена темы через меню и через диалог настроек;
  - проверка горячего применения настроек (тема/шрифт/интервалы);
  - экспорт одной и всех заметок во все форматы;
  - проверка сохранения настроек после перезапуска.
- Отчёт `TEST_REPORT_v050.md` фиксирует:
  - 100% успешное прохождение тестов (авто + ручные);
  - отсутствие критических дефектов;
  - несколько малозначительных ограничений (например, inline‑стили кнопок не всегда меняются без перезапуска).

### 8.3. Тестирование синхронизации и tombstones

Согласно `SYNC_IMPROVEMENTS.md` и `test_tombstones.py`:
- проверяется корректность маркировки заметок как `deleted=true`;
- проверяется распространение tombstones между устройствами;
- проверяется очистка tombstones старше заданного порога;
- проверяется обработка конфликтных файлов OneDrive и отсутствие «возрождения» удалённых заметок.

### 8.4. Обзор основных тестовых модулей

Ниже — краткий обзор содержимого директории `tests/`:

| Файл | Назначение | Критичность |
|------|------------|-------------|
| `test_v050_features.py` | Комплексный набор тестов для функций v0.5.0 (темы, настройки, экспорт, интервалы таймеров) | Высокая |
| `test_fixes_v050.py` | Тесты исправлений и регрессий, найденных в v0.5.0 | Средняя |
| `test_search.py` | Проверка поиска по заголовку/тексту/тегам, счётчика результатов, индикаторов и подсветки | Высокая |
| `test_highlight_all_fields.py` | Проверка, что подсветка найденного текста применяется ко всем полям (заголовок, тело, теги) | Средняя |
| `test_autosave.py` | Интерактивный тест автосохранения: проверка таймера, флага `has_unsaved_changes`, статуса и факта сохранения заметки | Высокая |
| `test_autosync.py` | Интерактивный тест автосинхронизации: проверка работы таймера, изменения интервала и появления статусов автосинхронизации | Высокая |
| `test_sync_search_interaction.py` | Тест сценария “поиск + синхронизация”: гарантирует отсутствие ложных `has_unsaved_changes` при автосинхронизации с активным поиском | Высокая |
| `test_ui_improvements.py` | Проверяет UI‑улучшения: поведение длинных заголовков, spacing в списке, индикаторы 📌/📄/🏷️ и их очистку | Средняя |
| `test_tags.py` | Проверяет корректный ввод/хранение тегов и их использование в поиске | Средняя |
| `test_tombstones.py` | Набор сценариев по tombstones: синхронизация удаления, слияние конфликтных файлов, очистка старых tombstones | Критическая |
| `test_auto_create_note.py` | Проверяет корректное создание новой заметки и начальное состояние полей/кнопок | Средняя |
| `test_manual_maximize.py`, `test_maximize_button.py` | Проверяют корректность работы режима максимизации окна и кнопки/горячих клавиш | Низкая |
| `test_ui_improvements.py` | Дополнительные проверки визуальных аспектов интерфейса | Средняя |

Часть тестов являются полу‑интерактивными (запускаются как отдельные скрипты и используют вывод в консоль и визуальную обратную связь); часть — автоматизируемые через `pytest` и `pytest‑qt` (используют фикстуру `qtbot`).

---
## 9. Хронология разработки и фазы проекта

Ниже — сжатая хронология, агрегирующая `CHANGELOG.md`, `ROADMAP.md` и `Отчёт по созданию`.

### 9.1. Фаза 1 — MVP и локальное хранилище (v0.1.0–0.2.0)

Период: 12–15 ноября 2025.

- Реализованы классы `Note` и `NoteStore`.
- Локальное хранение в JSON (`~/.notes_app/notes.json`).
- Базовый GUI на PySide6 (список заметок + редактор).
- CRUD‑операции, отслеживание несохранённых изменений.
- Логирование, валидация, резервное копирование и атомарная запись.
- Горячие клавиши: Ctrl+S, Ctrl+N, Ctrl+D.

### 9.2. Фаза 2 — Синхронизация и надёжность (v0.3.0)

Период: 15–16 ноября 2025.

- Введён `SyncManager` и алгоритм Last‑Writer‑Wins.
- Добавлена работа с облачной папкой (путь хранится в `config.json`).
- Асинхронная синхронизация с использованием потоков и Qt Signals.
- Обработка конфликтов, создание конфликтных заметок с префиксом `⚠️`.
- Внедрены tombstones для корректной синхронизации удаления.
- Реализовано автоматическое слияние конфликтных файлов OneDrive и их очистка.
- Ограничены размеры окна, улучшена верстка, добавлен перенос слов.

### 9.3. Фаза 3 — Автоматизация, поиск и теги (v0.4.0)

- Автосохранение по таймеру (по умолчанию 5 секунд). 
- Автосинхронизация по таймеру (по умолчанию 60 секунд).
- Поиск по заголовку, тексту и тегам, горячая клавиша Ctrl+F.
- Подсветка найденного текста системным цветом (QPalette.Highlight).
- Введение тегов: поле ввода, интеграция с поиском, метод получения всех уникальных тегов.
- Переработка UI (индикаторы результатов поиска, spacing в списке и др.).

### 9.4. Фаза 4 — Темы, настройки, экспорт (v0.5.0)

Фактические трудозатраты: ~8 часов.

- Создан модуль `themes.py` с `Theme` и `ThemeManager` и 4 предустановленными темами.
- Добавлено меню выбора темы («Вид → Тема») с radio‑кнопками.
- Реализован диалог настроек (`settings_dialog.py`) с тремя вкладками.
- Настраиваемые интервалы автосохранения и автосинхронизации.
- Настраиваемый шрифт и размер в редакторе.
- Экспорт одной и всех заметок в Markdown/TXT/HTML (+ ZIP).
- Написан и выполнен комплексный набор тестов v0.5.0 (авто + ручные).

### 9.5. Фаза 5 — Организация заметок (v0.6.0)

Фактические трудозатраты: ~6 часов.

- Закрепление заметок (pin) и приоритет закреплённых в списке.
- Панель статистики: всего, активных, закреплённых, удалённых.
- Расширенная сортировка по дате, алфавиту и размеру заметки.
- Поддержка поля `pinned` в модели и обратная совместимость.

### 9.6. (Фазы после v0.6.0)

Дальнейшие фазы (v0.7.0+), описанные в `ROADMAP.md` и разделе [Unreleased] `CHANGELOG.md`, относятся к планам развития и в данный момент не реализованы, поэтому в данном документе они опускаются.

---

## 10. Нефункциональные требования и качества системы

Сводка нефункциональных требований и качеств из кода, отчёта и дорожной карты.

### 10.1. Надёжность

- Атомарная запись JSON через временный файл и `Path.replace()`.
- Резервные копии при ошибках чтения/записи и при повреждении JSON.
- Логирование всех ключевых операций (`notes_app.log`).
- Tombstones вместо мгновенного удаления, что предотвращает «возвращение» записей.
- Автоматическое слияние конфликтных файлов OneDrive, чтобы не допустить рассинхронизации.

#### 10.1.1. Обработка ошибок и логирование

Обработка ошибок и логирование реализованы в первую очередь в `notes.py`, `sync.py` и `gui.py`:

- При ошибках чтения/записи JSON (`json.JSONDecodeError`, `IOError`, `OSError`) создаётся резервная копия повреждённого файла и выводится понятное сообщение в лог.
- При любой нештатной ситуации во время сохранения/синхронизации запись в лог содержит ID заметки (усечённый UUID) и краткое описание ошибки.
- В GUI‑слое критические ошибки при инициализации хранилища отображаются пользователю через `QMessageBox.critical`, после чего приложение безопасно завершается.

Типичные сообщения в `notes_app.log`:
- успешная загрузка/сохранение заметок и их количество;
- пометка заметки как удалённой и физическая очистка tombstones;
- обнаружение и обработка конфликтных файлов (`notes-DESKTOP-*.json`, `*conflicted*`);
- сообщения уровня WARNING/ERROR при некорректном формате даты, ID, заголовка или повреждённом JSON.

### 10.2. Масштабируемость и производительность

- Текущая реализация оптимизирована под сотни/тысячи заметок.
- Поиск реализован линейно по заголовку/тексту/тегам, чего достаточно при малых объёмах.
- В планах (ROADMAP): индексация и продвинутый поиск (регулярные выражения, фильтры по дате и тегам) для больших наборов данных.

### 10.3. Безопасность

- На текущий момент данные хранятся без шифрования, доступ ограничен правами пользователя ОС.

### 10.4. Удобство и UX

- Поддержка горячих клавиш (Ctrl+S/N/D/F/Q).
- Автосохранение и автосинхронизация по настраиваемым интервалам.
- Визуальные индикаторы поиска и синхронизации, панель статистики.
- Темы оформления и возможность изменения шрифта.
- Диалог настроек с «горячим» применением параметров без перезапуска.

---

## 11. Рекомендации для разработчиков (из CONTRIBUTING.md)

- Использовать виртуальное окружение и устанавливать зависимости из `requirements.txt`.
- Поддерживать существующую модульную архитектуру: разделение на модель (`notes.py`), GUI (`gui.py`), синхронизацию (`sync.py`), темы (`themes.py`), экспорт (`export.py`).
- При добавлении новой функциональности:
  - дополнять/обновлять тесты в `tests/`;
  - обновлять `CHANGELOG.md` и при необходимости `ROADMAP.md`;
  - добавлять/обновлять docstring‑и.
- Workflow контрибуции: fork → feature‑branch → PR с описанием изменений и ссылками на задачи/issue.

---

## 12. Известные ограничения и дальнейшие улучшения

- Некоторые inline‑стили кнопок могут не переключаться при смене темы без перезапуска (см. `TEST_REPORT_v050.md`).
- Цвет выделения найденного текста привязан к системному цвету и не меняется динамически при переключении темы.
- Облачные API (Dropbox/OneDrive/Google Drive) пока не используются — синхронизация осуществляется через файловую систему.

Планируется устранить эти ограничения и расширить функциональность в будущих версиях; конкретные реализации пока отсутствуют.

---
## 13. Заключение и текущий статус готовности

Данный документ агрегирует ключевую информацию из `README.md`, `PROJECT_STRUCTURE.md`, `CHANGELOG.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `BUILD_INSTRUCTIONS.md`, `SYNC_IMPROVEMENTS.md`, `TESTING_v050.md`, `TEST_REPORT_v050.md`, `Отчёт по созданию.ini` и структуры исходного кода. 

На текущий момент (v0.6.0):
- MVP и синхронизация реализованы и протестированы.
- Автоматизация, поиск, теги и базовая система тем — в боеспособном состоянии.
- UI‑кастомизация, настройки и экспорт — завершены и покрыты тестами v0.5.0.
- Организация заметок (закрепление, сортировка, статистика) — реализована и интегрирована в GUI.

Документ можно использовать как:
- техническую спецификацию и обоснование архитектурных решений;
- отчёт по созданию и эволюции информационной системы «Заметки»;
- основу для курсовой/дипломной работы и дальнейшего развития проекта Notes‑App.
