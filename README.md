# Notes App - Приложение для заметок с синхронизацией

Информационная система для создания и управления заметками с возможностью синхронизации между устройствами.

## Особенности

- 📝 Создание, редактирование и удаление заметок
- 💾 Локальное хранение в JSON формате
- 🔄 Синхронизация между устройствами через облачные сервисы
- 🖥️ Графический интерфейс на PySide6 (Qt)
- ⚡ Автосохранение изменений
- 📱 Оффлайн-работа с последующей синхронизацией

## Требования

- Python 3.9 - 3.13 (рекомендуется 3.13)
- PySide6

## Установка

### 1. Установите Python 3.13

Скачайте и установите Python 3.13 с официального сайта:
https://www.python.org/downloads/

### 2. Создайте виртуальное окружение

```powershell
# Для Windows PowerShell
py -3.13 -m venv venv
```

### 3. Активируйте виртуальное окружение

```powershell
# Если политики выполнения не позволяют активировать скрипты
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Активация
.\venv\Scripts\Activate.ps1
```

### 4. Установите зависимости

```powershell
pip install -r requirements.txt
```

## Структура проекта

```
Notes-App/
├── notes.py          # Модель данных (Note, NoteStore)
├── gui.py            # Графический интерфейс (PySide6)
├── sync.py           # Синхронизация между устройствами
├── main.py           # Точка входа в приложение
├── requirements.txt  # Зависимости проекта
└── README.md         # Этот файл
```

## Использование

### Запуск приложения

После активации виртуального окружения:

```powershell
# Активация виртуального окружения (если еще не активировано)
.\venv\Scripts\Activate.ps1

# Запуск приложения
python main.py
```

Или напрямую через интерпретатор виртуального окружения:

```powershell
C:/Users/ilyak/OneDrive/Desktop/Notes-App/.venv/Scripts/python.exe main.py
```

### Тестирование модуля заметок

```powershell
python notes.py
```

### Функции приложения

- **Создание заметки**: Нажмите "➕ Создать заметку"
- **Редактирование**: Выберите заметку из списка, измените текст
- **Сохранение**: Нажмите "💾 Сохранить" или Ctrl+S
- **Удаление**: Нажмите "🗑️ Удалить"
- **Автосохранение**: Изменения отслеживаются автоматически

## Архитектура

### Модель заметок (notes.py)

- **Note**: Класс заметки с полями:
  - `id` - UUID заметки
  - `title` - Заголовок
  - `body` - Текст
  - `last_modified` - Время последнего изменения
  - `version` - Версия заметки

- **NoteStore**: Класс для управления заметками:
  - CRUD операции (создание, чтение, обновление, удаление)
  - Сохранение/загрузка из JSON
  - Хранение в `~/.notes_app/notes.json`

### GUI (gui.py)

- Список заметок (QListWidget)
- Редактор заметки (QLineEdit + QTextEdit)
- Кнопки управления (Создать, Сохранить, Удалить, Синхронизировать)

### Синхронизация (sync.py)

- Алгоритм Last-Writer-Wins (LWW)
- Обработка конфликтов
- Работа с облачными папками (Dropbox, Google Drive, OneDrive)

## Хранение данных

Заметки хранятся локально в JSON формате:

```json
{
  "notes": {
    "uuid1": {
      "id": "uuid1",
      "title": "Заголовок",
      "body": "Текст",
      "last_modified": "2025-11-10T12:00:00Z",
      "version": 1
    }
  },
  "meta": {
    "created": "2025-11-10T12:00:00Z",
    "count": 1
  }
}
```

## Дорожная карта

### MVP (Завершено)
- [x] Реализация модели данных (Note, NoteStore)
- [x] Создание графического интерфейса на PySide6
- [x] CRUD операции (создание, чтение, обновление, удаление)
- [x] Отслеживание несохраненных изменений
- [x] Сортировка заметок по дате изменения

### В разработке
- [ ] Реализация синхронизации (Last-Writer-Wins)
- [ ] Автосохранение по таймеру
- [ ] Обработка конфликтов при синхронизации

### Будущие улучшения
- [ ] Поиск по заметкам
- [ ] Шифрование данных
- [ ] Интеграция с облачными API (Dropbox, Google Drive)
- [ ] Горячие клавиши (Ctrl+S, Ctrl+N, Ctrl+F)
- [ ] Экспорт в Markdown/PDF
- [ ] Теги и категории

## Лицензия

MIT License

## Автор

Проект создан на основе отчёта по проектированию информационной системы «Заметки».
